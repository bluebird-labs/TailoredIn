{
  "permissions": {
    "defaultMode": "plan",
    "additionalDirectories": ["~/Documents"],
    "allow": ["EnterPlanMode", "WebFetch"]
  },
  "hooks": {
    "WorktreeCreate": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.worktree_path' | { read -r wt; main=$(git worktree list --porcelain | head -1 | sed 's/^worktree //'); [ -f \"$main/.env\" ] && cp \"$main/.env\" \"$wt/.env\" && echo '{\"systemMessage\": \"Copied .env to worktree\"}'; }",
            "statusMessage": "Copying .env to worktree..."
          }
        ]
      }
    ]
  }
}
