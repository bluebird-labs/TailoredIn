{
  "name": "tailoredin",
  "version": "0.1.0",
  "devDependencies": {
    "@biomejs/biome": "latest",
    "dependency-cruiser": "^17",
    "knip": "^6.1.0"
  },
  "private": true,
  "scripts": {
    "api": "bun run api/src/index.ts",
    "api:dev": "bun --env-file=${BUN_ENV_FILE:-.env} --watch api/src/index.ts",
    "dev": "bun run --parallel api:dev web:dev",
    "robot": "bun run cli/src/robot/index.ts",
    "robot:dev": "bun --watch cli/src/robot/index.ts",
    "cvs": "bun run cli/src/cvs/index.ts",
    "web": "bun run --cwd web preview",
    "web:dev": "bun --env-file=${BUN_ENV_FILE:-.env} run --cwd web dev",
    "web:build": "bun run --cwd web build",
    "check": "biome check domain application infrastructure core api cli web",
    "check:fix": "biome check --write domain application infrastructure core api cli web",
    "format": "biome format --write domain application infrastructure core api cli web",
    "lint": "biome lint domain application infrastructure core api cli web",
    "typecheck": "bun run --filter '@tailoredin/*' typecheck",
    "test": "bun run --filter '@tailoredin/*' test",
    "test:coverage": "bun run --filter '@tailoredin/*' test:coverage",
    "knip": "knip",
    "dep:check": "depcruise domain application infrastructure api cli web --config .dependency-cruiser.cjs",
    "dep:graph": "depcruise domain application infrastructure api cli web --config .dependency-cruiser.cjs --output-type dot | dot -T svg > dep-graph.svg",
    "api:lint": "biome check api",
    "api:lint:fix": "biome check --write api",
    "api:typecheck": "bun run --cwd api typecheck",
    "api:test": "bun run --cwd api test",
    "cli:lint": "biome check cli",
    "cli:lint:fix": "biome check --write cli",
    "cli:typecheck": "bun run --cwd cli typecheck",
    "cli:test": "bun run --cwd cli test",
    "db:migration:create": "bun --env-file=${BUN_ENV_FILE:-.env} run --cwd infrastructure migration:create",
    "db:migration:up": "bun --env-file=${BUN_ENV_FILE:-.env} run --cwd infrastructure migration:up",
    "db:seed": "bun --env-file=${BUN_ENV_FILE:-.env} run --cwd infrastructure seed:run"
  },
  "workspaces": [
    "domain",
    "application",
    "infrastructure",
    "core",
    "api",
    "cli",
    "web"
  ]
}
